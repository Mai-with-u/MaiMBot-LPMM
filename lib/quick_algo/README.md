# QuickAlgo
这是一个快速算法库，旨在为LPMM提供Graph数据结构的高效实现和一些复杂算法的高效实现。

## 目录结构

## 构建
请在当前目录下执行`build_lib.bat`/`build_lib.sh`，这将在本目录下构建本依赖库。

在构建之前，请确保您的电脑上装有以下依赖：

- MSVC（Windows）/GCC（Linux）
- Python 3.12
- Cython（Python包，通过pip安装）